# Generated by One CLI
name: CI - ai-core

on:
  pull_request:
    paths:
      - 'packages/ai-core/**'
      - '.github/workflows/ci-packages-ai-core.yml'
  push:
    paths:
      - 'packages/ai-core/**'
      - '.github/workflows/ci-packages-ai-core.yml'
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./packages/ai-core
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: ./packages/ai-core/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run pnpm run check
        run: pnpm run check
      - name: Run pnpm run test
        run: pnpm run test
      - name: Run pnpm run build
        run: pnpm run build
