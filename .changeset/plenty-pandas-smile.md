---
"@ai-novel-diagnosis/ai-core": minor
---

Add an optional `verification` summary (attempted / skipped / rejected / unavailable / verified counters) to `StoryAuditResult`, populated when story-audit findings go through an LLM second-pass verifier.
