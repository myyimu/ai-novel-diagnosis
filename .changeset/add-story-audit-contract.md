---
"@ai-novel-diagnosis/ai-core": minor
---

Add story audit (故事体检) shared contracts — `StoryAuditResult`, `StoryAuditFinding`, `StoryAuditFindingReview`, `DialogueStatistics`, evidence/scene/event/fact types, plus `BookAnalysisPurpose`/`StoryAuditProfile` and the deterministic `computeDialogueStatistics` parser that handles curly/corner/double-corner/ascii quotes, nesting, cross-paragraph dialogue, unclosed-quote warnings, and excludable content kinds.
