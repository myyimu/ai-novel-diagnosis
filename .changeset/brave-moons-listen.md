---
"@ai-novel-diagnosis/ai-core": minor
---

Add the `PremiseReviewResult` contract for 立项审稿 (premise review): a three-state `engineVerdict` (`solid` / `fixable` / `not-worth-writing`), the four story-engine audit layers (engine / desire / conflict / irreducibility), cliché findings whose evidence quotes the author's own premise text, and up to three upgrade directions (情感 / 权谋 / 战争) that may only replace the core conflict.
