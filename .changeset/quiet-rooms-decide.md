---
"@ai-novel-diagnosis/ai-core": minor
---

Add an optional `recordId` to `PremiseConsultResult` and `ReportDivergenceResult`, set by the API layer when a consultation or divergence detection is persisted into a project's medical record. Pure builders never fabricate it.
