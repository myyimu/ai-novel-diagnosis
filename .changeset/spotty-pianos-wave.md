---
"@ai-novel-diagnosis/ai-core": minor
---

Add revision iteration pure functions (`hashRevisionText`, `normalizeRevisionChapterTitle`, `createRevisionVersion`, `findRevisionVersionForDraft`, `findPreviousRevisionVersion`, `hasComparableQuickScore`, `buildRevisionComparison`) shared by the web workspace and the API revision retest endpoint, so both sides derive identical version ids and adjacent-revision comparisons.
