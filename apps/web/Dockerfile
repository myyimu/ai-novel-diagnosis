# Generated by One CLI
FROM node:20-alpine

WORKDIR /workspace
RUN corepack enable

COPY package.json ./
COPY pnpm-lock.yaml* package-lock.json* yarn.lock* ./
RUN pnpm install

COPY . .

EXPOSE 3000
CMD ["sh", "-c", "pnpm run dev -- --hostname 0.0.0.0 --port 3000"]
